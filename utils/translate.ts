/**
 * A placeholder for translate function (leaves given input as-is).
 * Use to wrap all human-readable labels, instead of hard-coding them.
 * That way it's easy to switch it later for a proper i18n tool, if needed.
 */
export const T = (text: string): string => {
  return text
}
